export const createSoybean = () => {
    const newSeed = {}
    newSeed.type = 'Soybean'
    newSeed.height = 20
    newSeed.output = 4
    return newSeed
}
//let newSoybean = createSoybean()
